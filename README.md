Capybara and Selenium WebDriver sample
======================================

A sample using RSpec, Capybara and Selenium WebDriver.


```sh
$ docker build --tag capybara-and-selenium .
$ docker run --interactive --tty --rm --volume "$PWD":/app capybara-and-selenium /bin/bash
$ rspec
```
